# Machine-specific paths (e.g. Flutter SDK) belong in ~/.zsh_local — see .zsh_custom/.local.zsh.example
# export PATH="/path/to/flutter/bin:$PATH"

if [[ -f "$HOME/.cargo/env" ]]; then . "$HOME/.cargo/env"; fi
